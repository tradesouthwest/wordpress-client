/* data-date + 14 days */
(function( $ ) {
  "use strict";
$(document).ready( function(){
/*const today = new Date();
const fourteenDaysFromNow = new Date(today.getDate() + 14);

const elements = document.querySelectorAll("a[data-date]");

for (const element of elements) {
  const dayNumber = parseInt(element.getAttribute("data-date"));

  if (dayNumber === fourteenDaysFromNow.getDate()) {
    element.classList.add("ui-datepicker-unselectable");
  }
} 
console.log(fourteenDaysFromNow); 
*/
/*    $(function returndate_autofill() {
        $('#quickcab_form_departure_date_18').keyup(function () {
            $('#quickcab_form_return_departure_date_autofill').val( $(this).val() );
               console.log("it is " + $(this).val());
               
        });
        
    }); isInDisabledDates .disabledDates */
       /*   
        $('a.ui-state-default').picker({
            disabledDates: function(date){
                var string = jQuery.picker.formatDate('YYYY-MM-DD', date);
                return [ array.indexOf(string) == false ]
            }
        });    
*/
    
  });
})(jQuery);
